{
    "items": [
        [
            "Details",
            "status",
            "EMBEDDED(Users.name)",
            "EMBEDDED(Users)",
            "EMBEDDED(name)",
            "EMBEDDED(Use.name)",
            "EMBEDDED(Users.)"
        ]
    ]
}